The Adapters layer will contain logic for communicating with external services such as third party APIs.

For example, we might define an ISmsService interface with a method for sending an SMS message.
